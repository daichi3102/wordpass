<% informations = @informations.where.not(visitor_id: current_user.id) %>
<% if informations.exists? %>
  <%= render partial: 'information', collection: informations, as: :information %>
  <%= paginate informations %>
<% else %>
  <p>
  通知はありません
  </p>
<% end %>
